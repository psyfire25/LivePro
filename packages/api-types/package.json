{
  "name": "@livepro/api-types",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "gen": "openapi-typescript ./schema.json -o src/types.ts",
    "build": "tsc -p tsconfig.json && cp schema.json dist/schema.json",
    "prepublishOnly": "pnpm run build",
    "test": "echo \"API types validated during build\""
  }
}